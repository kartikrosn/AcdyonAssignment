{
  "name": "jobpulse-backend",
  "version": "1.0.0",
  "description": "jobPulse backend — real-data job ingestion from Greenhouse",
  "main": "src/server.js",
  "type": "module",
  "scripts": {
    "dev": "node --watch src/server.js",
    "start": "prisma migrate deploy && node src/server.js",
    "db:migrate": "prisma migrate dev",
    "db:studio": "prisma studio",
    "db:generate": "prisma generate"
  },
  "dependencies": {
    "@prisma/client": "^5.14.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "express-validator": "^7.1.0",
    "helmet": "^7.1.0",
    "pino": "^9.2.0",
    "pino-pretty": "^11.2.1",
    "prisma": "^5.14.0"
  },
  "devDependencies": {}
}
